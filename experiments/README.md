Place experiments here. Each experiment should have a directory with a `config.yaml`, `run.sh`, and `notes.md`.

Example:

```
experiments/exp-001-small-transformer/
  config.yaml
  run.sh
  notes.md
  results/
```
